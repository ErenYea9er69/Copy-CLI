{
  "name": "copyshed-vscode",
  "displayName": "CopyShed",
  "description": "Preview and apply on-brand copy rewrites as you save, using your project's copyshed.config.json.",
  "version": "0.1.0",
  "publisher": "your-team",
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Linters", "Formatters"],
  "activationEvents": ["onLanguage:typescriptreact", "onLanguage:javascriptreact", "onLanguage:json"],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copyshed.previewFile",
        "title": "CopyShed: Preview copy rewrites for this file"
      },
      {
        "command": "copyshed.applyFile",
        "title": "CopyShed: Apply copy rewrites to this file"
      }
    ],
    "configuration": {
      "title": "CopyShed",
      "properties": {
        "copyshed.applyOnSave": {
          "type": "boolean",
          "default": false,
          "description": "Run `copyshed apply --yes --file <saved file>` automatically on save. Off by default: preview first."
        },
        "copyshed.cliPath": {
          "type": "string",
          "default": "copyshed",
          "description": "Path to the copyshed CLI binary. Defaults to the one on your PATH."
        }
      }
    }
  }
}
